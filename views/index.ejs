<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Vedant Sareen — Cybersecurity Engineer</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=JetBrains+Mono:wght@300;400;500&family=Orbitron:wght@400;700;900&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
<style>
:root{
  --void:#020408;
  --deep:#050d18;
  --surface:rgba(8,20,40,0.7);
  --glass:rgba(12,30,60,0.4);
  --neon-blue:#00d4ff;
  --neon-purple:#b44dff;
  --neon-green:#00ff9d;
  --neon-red:#ff3366;
  --text:#e2eaf8;
  --muted:#5a7a9a;
  --border:rgba(0,212,255,0.12);
  --glow-blue:0 0 20px rgba(0,212,255,0.4),0 0 60px rgba(0,212,255,0.15);
  --glow-purple:0 0 20px rgba(180,77,255,0.4),0 0 60px rgba(180,77,255,0.15);
}
*{margin:0;padding:0;box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  background:var(--void);
  color:var(--text);
  font-family:'Syne',sans-serif;
  overflow-x:hidden;
  cursor:none;
}
::selection{background:rgba(0,212,255,0.3);color:#fff}
::-webkit-scrollbar{width:3px}
::-webkit-scrollbar-track{background:var(--void)}
::-webkit-scrollbar-thumb{background:var(--neon-blue);border-radius:2px}

/* CURSOR */
#cursor{position:fixed;width:12px;height:12px;background:var(--neon-blue);border-radius:50%;pointer-events:none;z-index:9999;transform:translate(-50%,-50%);transition:transform 0.1s,background 0.2s;mix-blend-mode:screen;box-shadow:var(--glow-blue)}
#cursor-ring{position:fixed;width:40px;height:40px;border:1px solid rgba(0,212,255,0.5);border-radius:50%;pointer-events:none;z-index:9998;transform:translate(-50%,-50%);transition:transform 0.15s ease,width 0.3s,height 0.3s,opacity 0.3s}
body:hover #cursor-ring{opacity:1}

/* PRELOADER */
#preloader{
  position:fixed;inset:0;z-index:99999;
  background:var(--void);
  display:flex;align-items:center;justify-content:center;flex-direction:column;
  gap:24px;
}
.preload-text{
  font-family:'Orbitron',monospace;
  font-size:clamp(10px,1.5vw,14px);
  letter-spacing:4px;
  color:var(--neon-blue);
  opacity:0;
}
.preload-bar-wrap{width:280px;height:1px;background:rgba(0,212,255,0.15);position:relative;overflow:hidden}
.preload-bar{height:100%;width:0%;background:linear-gradient(90deg,var(--neon-blue),var(--neon-purple));box-shadow:var(--glow-blue);transition:width 0.05s}
.preload-pct{font-family:'JetBrains Mono',monospace;font-size:12px;color:var(--muted)}

/* CANVAS */
#nebula{position:fixed;inset:0;z-index:0;opacity:0.6}

/* GRID OVERLAY */
.grid-overlay{
  position:fixed;inset:0;z-index:1;pointer-events:none;
  background-image:
    linear-gradient(rgba(0,212,255,0.03) 1px,transparent 1px),
    linear-gradient(90deg,rgba(0,212,255,0.03) 1px,transparent 1px);
  background-size:80px 80px;
  mask-image:radial-gradient(ellipse 80% 80% at 50% 50%,black 40%,transparent 100%);
}

/* NAV */
nav{
  position:fixed;top:0;left:0;right:0;z-index:1000;
  padding:20px 6%;
  display:flex;align-items:center;justify-content:space-between;
  border-bottom:1px solid transparent;
  transition:all 0.4s;
}
nav.scrolled{
  background:rgba(2,4,8,0.85);
  backdrop-filter:blur(20px);
  border-bottom-color:var(--border);
}
.nav-logo{
  font-family:'Orbitron',monospace;
  font-size:16px;font-weight:700;
  color:var(--neon-blue);
  letter-spacing:3px;
  text-decoration:none;
}
.nav-links{display:flex;gap:36px;list-style:none}
.nav-links a{
  font-family:'JetBrains Mono',monospace;
  font-size:11px;letter-spacing:2px;
  color:var(--muted);text-decoration:none;
  transition:color 0.3s;
  position:relative;
}
.nav-links a::after{
  content:'';position:absolute;bottom:-4px;left:0;width:0;height:1px;
  background:var(--neon-blue);transition:width 0.3s;
}
.nav-links a:hover{color:var(--neon-blue)}
.nav-links a:hover::after{width:100%}

/* SECTIONS */
section{position:relative;z-index:2;padding:120px 8%}

/* HERO */
#hero{
  min-height:100vh;display:flex;align-items:center;
  padding:0 8%;position:relative;overflow:hidden;
}
.hero-grid-num{
  position:absolute;right:6%;top:50%;transform:translateY(-50%);
  font-family:'JetBrains Mono',monospace;font-size:11px;
  color:rgba(0,212,255,0.15);letter-spacing:3px;
  writing-mode:vertical-rl;
  display:flex;flex-direction:column;gap:12px;align-items:center;
}
.hero-grid-num::before{
  content:'';width:1px;height:80px;
  background:linear-gradient(to bottom,transparent,var(--neon-blue));
  display:block;
}
.hero-content{max-width:900px}
.hero-badge{
  display:inline-flex;align-items:center;gap:8px;
  font-family:'JetBrains Mono',monospace;font-size:11px;
  letter-spacing:3px;color:var(--neon-green);
  margin-bottom:28px;opacity:0;
}
.hero-badge::before{content:'';width:6px;height:6px;border-radius:50%;background:var(--neon-green);animation:pulse 2s infinite;box-shadow:0 0 10px var(--neon-green)}
@keyframes pulse{0%,100%{opacity:1;transform:scale(1)}50%{opacity:0.5;transform:scale(1.4)}}
.hero-name{
  font-family:'Orbitron',monospace;
  font-size:clamp(48px,8vw,110px);
  font-weight:900;line-height:0.9;
  letter-spacing:-2px;
  background:linear-gradient(135deg,#fff 0%,var(--neon-blue) 50%,var(--neon-purple) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;
  background-clip:text;
  opacity:0;transform:translateY(60px);
}
.hero-name span{display:block}
.hero-role{
  margin-top:24px;
  font-family:'JetBrains Mono',monospace;
  font-size:clamp(14px,2vw,18px);
  color:var(--muted);letter-spacing:2px;
  opacity:0;
}
.hero-role em{color:var(--neon-blue);font-style:normal}
#typed-text{color:var(--neon-blue)}
.cursor-blink{animation:blink 1s infinite}
@keyframes blink{0%,100%{opacity:1}50%{opacity:0}}
.hero-desc{
  margin-top:28px;max-width:580px;
  font-size:15px;line-height:1.8;
  color:rgba(226,234,248,0.6);
  opacity:0;
}
.hero-cta{
  display:flex;gap:16px;margin-top:44px;opacity:0;
}
.btn-primary{
  font-family:'JetBrains Mono',monospace;
  font-size:12px;letter-spacing:3px;
  padding:14px 32px;border:none;
  background:linear-gradient(135deg,var(--neon-blue),var(--neon-purple));
  color:#fff;cursor:none;
  clip-path:polygon(8px 0%,100% 0%,calc(100% - 8px) 100%,0% 100%);
  transition:transform 0.2s,box-shadow 0.3s;
  text-decoration:none;display:inline-block;
}
.btn-primary:hover{transform:translateY(-3px);box-shadow:var(--glow-blue)}
.btn-ghost{
  font-family:'JetBrains Mono',monospace;
  font-size:12px;letter-spacing:3px;
  padding:13px 32px;
  border:1px solid rgba(0,212,255,0.3);
  color:var(--neon-blue);cursor:none;background:transparent;
  clip-path:polygon(8px 0%,100% 0%,calc(100% - 8px) 100%,0% 100%);
  transition:all 0.3s;text-decoration:none;display:inline-block;
}
.btn-ghost:hover{background:rgba(0,212,255,0.08);box-shadow:var(--glow-blue);border-color:var(--neon-blue)}
.hero-stats{
  display:flex;gap:48px;margin-top:60px;
  padding-top:40px;border-top:1px solid var(--border);
  opacity:0;
}
.stat-item{text-align:left}
.stat-num{
  font-family:'Orbitron',monospace;
  font-size:32px;font-weight:700;
  color:var(--neon-blue);
}
.stat-label{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted);letter-spacing:2px;margin-top:4px}
.scroll-hint{
  position:absolute;bottom:40px;left:50%;transform:translateX(-50%);
  display:flex;flex-direction:column;align-items:center;gap:8px;
  font-family:'JetBrains Mono',monospace;font-size:10px;
  color:var(--muted);letter-spacing:3px;opacity:0;
  animation:float 2s ease-in-out infinite;
}
@keyframes float{0%,100%{transform:translateX(-50%) translateY(0)}50%{transform:translateX(-50%) translateY(-8px)}}
.scroll-line{width:1px;height:50px;background:linear-gradient(to bottom,var(--neon-blue),transparent)}

/* SECTION HEADER */
.section-header{margin-bottom:70px;position:relative}
.section-tag{
  font-family:'JetBrains Mono',monospace;font-size:11px;
  color:var(--neon-purple);letter-spacing:4px;
  margin-bottom:12px;display:block;
}
.section-title{
  font-family:'Orbitron',monospace;
  font-size:clamp(28px,4vw,52px);font-weight:700;
  line-height:1.1;
}
.section-title em{font-style:normal;color:var(--neon-blue)}
.section-line{
  width:60px;height:2px;
  background:linear-gradient(90deg,var(--neon-blue),transparent);
  margin-top:16px;
}

/* ABOUT */
#about{padding:100px 8%}
.about-grid{display:grid;grid-template-columns:1fr 1fr;gap:80px;align-items:center}
.about-visual{position:relative}
.about-hex{
  width:100%;aspect-ratio:1;
  background:var(--glass);
  border:1px solid var(--border);
  clip-path:polygon(50% 0%,100% 25%,100% 75%,50% 100%,0% 75%,0% 25%);
  display:flex;align-items:center;justify-content:center;
  backdrop-filter:blur(20px);
  position:relative;overflow:hidden;
  max-width:340px;margin:0 auto;
}
.about-hex::before{
  content:'';position:absolute;inset:0;
  background:radial-gradient(circle at 30% 30%,rgba(0,212,255,0.1),transparent 60%),
             radial-gradient(circle at 70% 70%,rgba(180,77,255,0.1),transparent 60%);
}
.about-hex-inner{
  font-family:'JetBrains Mono',monospace;
  font-size:11px;color:var(--neon-blue);
  text-align:center;padding:20px;line-height:2;
  position:relative;z-index:1;
}
.about-content p{
  font-size:15px;line-height:1.9;
  color:rgba(226,234,248,0.7);margin-bottom:20px;
}
.about-tags{display:flex;flex-wrap:wrap;gap:10px;margin-top:28px}
.tag{
  font-family:'JetBrains Mono',monospace;font-size:10px;
  letter-spacing:2px;padding:6px 14px;
  border:1px solid var(--border);color:var(--neon-blue);
  clip-path:polygon(4px 0%,100% 0%,calc(100% - 4px) 100%,0% 100%);
  background:rgba(0,212,255,0.05);
}

/* SKILLS */
#skills{background:linear-gradient(180deg,transparent 0%,rgba(0,20,40,0.3) 50%,transparent 100%)}
.skills-container{display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:24px}
.skill-card{
  background:var(--glass);
  border:1px solid var(--border);
  backdrop-filter:blur(20px);
  padding:28px;
  position:relative;overflow:hidden;
  transition:transform 0.3s,border-color 0.3s;
  clip-path:polygon(0 0,calc(100% - 16px) 0,100% 16px,100% 100%,0 100%);
}
.skill-card::before{
  content:'';position:absolute;top:0;left:0;right:0;height:1px;
  background:linear-gradient(90deg,var(--neon-blue),transparent);
  opacity:0;transition:opacity 0.3s;
}
.skill-card:hover{transform:translateY(-6px);border-color:rgba(0,212,255,0.3)}
.skill-card:hover::before{opacity:1}
.skill-card-icon{font-size:24px;margin-bottom:16px}
.skill-card-title{
  font-family:'Orbitron',monospace;font-size:13px;
  letter-spacing:2px;color:var(--neon-blue);margin-bottom:16px;
}
.skill-list{display:flex;flex-wrap:wrap;gap:8px}
.skill-chip{
  font-family:'JetBrains Mono',monospace;font-size:10px;
  letter-spacing:1px;padding:4px 10px;
  background:rgba(0,212,255,0.06);
  border:1px solid rgba(0,212,255,0.15);
  color:rgba(226,234,248,0.7);
  transition:all 0.2s;
}
.skill-chip:hover{background:rgba(0,212,255,0.15);color:var(--neon-blue);border-color:rgba(0,212,255,0.4)}

/* ORBIT */
.orbit-section{display:flex;align-items:center;justify-content:center;gap:80px;margin-top:80px;flex-wrap:wrap}
.orbit-container{
  position:relative;width:320px;height:320px;
}
.orbit-core{
  position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);
  width:80px;height:80px;border-radius:50%;
  background:radial-gradient(circle,rgba(0,212,255,0.3),rgba(0,212,255,0.05));
  border:1px solid var(--neon-blue);
  display:flex;align-items:center;justify-content:center;
  font-family:'Orbitron',monospace;font-size:8px;
  color:var(--neon-blue);letter-spacing:2px;text-align:center;
  box-shadow:var(--glow-blue);
}
.orbit-ring{
  position:absolute;top:50%;left:50%;
  border-radius:50%;border:1px solid rgba(0,212,255,0.1);
  transform:translate(-50%,-50%);
}
.orbit-planet{
  position:absolute;width:44px;height:44px;
  background:var(--glass);border:1px solid rgba(0,212,255,0.3);
  border-radius:50%;display:flex;align-items:center;justify-content:center;
  font-size:16px;cursor:none;
  transition:transform 0.3s,box-shadow 0.3s;
  backdrop-filter:blur(10px);
}
.orbit-planet:hover{transform:scale(1.3);box-shadow:var(--glow-blue)}
.orbit-label-list{display:flex;flex-direction:column;gap:12px}
.orbit-label{
  display:flex;align-items:center;gap:12px;
  font-family:'JetBrains Mono',monospace;font-size:12px;
  color:rgba(226,234,248,0.6);
}
.orbit-dot{width:6px;height:6px;border-radius:50%;background:var(--neon-blue);flex-shrink:0;box-shadow:0 0 8px var(--neon-blue)}
.orbit-dot.purple{background:var(--neon-purple);box-shadow:0 0 8px var(--neon-purple)}
.orbit-dot.green{background:var(--neon-green);box-shadow:0 0 8px var(--neon-green)}

/* PROJECTS */
.projects-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(340px,1fr));gap:28px}
.project-card{
  background:var(--glass);
  border:1px solid var(--border);
  backdrop-filter:blur(20px);
  padding:32px;
  position:relative;overflow:hidden;
  cursor:none;
  transition:all 0.4s;
  transform-style:preserve-3d;
}
.project-card::after{
  content:'';position:absolute;inset:0;
  background:radial-gradient(circle at var(--mx,50%) var(--my,50%),rgba(0,212,255,0.06),transparent 60%);
  opacity:0;transition:opacity 0.3s;pointer-events:none;
}
.project-card:hover::after{opacity:1}
.project-card:hover{transform:translateY(-8px);border-color:rgba(0,212,255,0.3);box-shadow:0 24px 60px rgba(0,0,0,0.4)}
.project-num{
  font-family:'Orbitron',monospace;font-size:48px;font-weight:900;
  color:rgba(0,212,255,0.06);position:absolute;right:24px;top:16px;
  line-height:1;
}
.project-category{
  font-family:'JetBrains Mono',monospace;font-size:10px;
  letter-spacing:3px;color:var(--neon-purple);margin-bottom:12px;
  display:flex;align-items:center;gap:8px;
}
.project-category::before{content:'';width:20px;height:1px;background:var(--neon-purple)}
.project-title{
  font-family:'Syne',sans-serif;font-size:20px;font-weight:700;
  line-height:1.3;margin-bottom:14px;
}
.project-desc{
  font-size:13px;line-height:1.8;
  color:rgba(226,234,248,0.55);margin-bottom:20px;
}
.project-techs{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:24px}
.tech-badge{
  font-family:'JetBrains Mono',monospace;font-size:10px;
  padding:3px 10px;background:rgba(180,77,255,0.08);
  border:1px solid rgba(180,77,255,0.2);color:var(--neon-purple);
}
.project-highlights{
  list-style:none;font-size:12px;
  color:rgba(226,234,248,0.5);
  border-top:1px solid var(--border);padding-top:16px;
}
.project-highlights li{padding:4px 0;padding-left:16px;position:relative}
.project-highlights li::before{
  content:'▹';position:absolute;left:0;
  color:var(--neon-blue);font-size:10px;
}

/* RESEARCH */
.research-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:24px}
.research-card{
  background:var(--glass);
  border:1px solid var(--border);
  backdrop-filter:blur(20px);
  padding:28px;position:relative;overflow:hidden;
  transition:all 0.3s;
  clip-path:polygon(0 0,100% 0,100% calc(100% - 16px),calc(100% - 16px) 100%,0 100%);
}
.research-card:hover{border-color:rgba(180,77,255,0.4);transform:translateY(-4px)}
.research-card::before{
  content:'';position:absolute;bottom:0;right:0;
  width:16px;height:16px;
  background:linear-gradient(135deg,transparent 50%,rgba(180,77,255,0.4) 50%);
}
.research-type{
  font-family:'JetBrains Mono',monospace;font-size:10px;
  letter-spacing:3px;color:var(--neon-purple);margin-bottom:12px;
}
.research-title{font-size:16px;font-weight:600;line-height:1.4;margin-bottom:12px}
.research-meta{
  font-family:'JetBrains Mono',monospace;font-size:11px;
  color:var(--muted);
}

/* ACHIEVEMENTS */
.achievements-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:20px}
.achievement-card{
  background:var(--glass);
  border:1px solid var(--border);
  backdrop-filter:blur(16px);
  padding:28px 24px;
  text-align:center;
  position:relative;overflow:hidden;
  transition:all 0.3s;
}
.achievement-card::before{
  content:'';position:absolute;top:0;left:0;right:0;height:2px;
  background:linear-gradient(90deg,transparent,var(--neon-blue),transparent);
  opacity:0;transition:opacity 0.3s;
}
.achievement-card:hover{transform:translateY(-6px);border-color:rgba(0,212,255,0.3)}
.achievement-card:hover::before{opacity:1}
.achievement-icon{font-size:32px;margin-bottom:16px;display:block}
.achievement-value{
  font-family:'Orbitron',monospace;font-size:24px;font-weight:700;
  color:var(--neon-blue);margin-bottom:8px;
}
.achievement-label{font-size:13px;color:rgba(226,234,248,0.6);line-height:1.5}

/* TIMELINE */
.timeline{position:relative;padding-left:40px}
.timeline::before{
  content:'';position:absolute;left:0;top:0;bottom:0;
  width:1px;background:linear-gradient(to bottom,var(--neon-blue),rgba(0,212,255,0.1));
}
.timeline-item{position:relative;margin-bottom:48px}
.timeline-dot{
  position:absolute;left:-46px;top:4px;
  width:12px;height:12px;border-radius:50%;
  background:var(--neon-blue);
  box-shadow:0 0 12px var(--neon-blue),0 0 24px rgba(0,212,255,0.3);
}
.timeline-year{
  font-family:'JetBrains Mono',monospace;font-size:11px;
  color:var(--neon-purple);letter-spacing:2px;margin-bottom:8px;
}
.timeline-title{font-size:18px;font-weight:700;margin-bottom:4px}
.timeline-sub{font-family:'JetBrains Mono',monospace;font-size:12px;color:var(--muted);margin-bottom:10px}
.timeline-desc{font-size:13px;color:rgba(226,234,248,0.55);line-height:1.7}

/* CERTIFICATIONS */
.cert-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:16px;margin-top:60px}
.cert-card{
  background:rgba(0,212,255,0.04);
  border:1px solid rgba(0,212,255,0.12);
  padding:20px;text-align:center;
  transition:all 0.3s;
}
.cert-card:hover{background:rgba(0,212,255,0.08);border-color:rgba(0,212,255,0.3);transform:scale(1.03)}
.cert-icon{font-size:28px;margin-bottom:10px}
.cert-name{font-family:'JetBrains Mono',monospace;font-size:11px;letter-spacing:1px;color:var(--neon-blue);margin-bottom:4px}
.cert-status{font-size:11px;color:var(--muted)}

/* CONTACT */
#contact{text-align:center}
.contact-wrapper{max-width:700px;margin:0 auto}
.contact-tagline{
  font-size:clamp(14px,2vw,18px);
  color:rgba(226,234,248,0.5);
  margin-bottom:50px;line-height:1.8;
}
.contact-links{display:flex;justify-content:center;gap:20px;flex-wrap:wrap;margin-bottom:60px}
.contact-link{
  display:flex;align-items:center;gap:12px;
  font-family:'JetBrains Mono',monospace;font-size:12px;
  letter-spacing:2px;color:var(--text);text-decoration:none;
  padding:16px 28px;
  background:var(--glass);border:1px solid var(--border);
  backdrop-filter:blur(10px);
  transition:all 0.3s;
  clip-path:polygon(8px 0%,100% 0%,calc(100% - 8px) 100%,0% 100%);
}
.contact-link:hover{
  border-color:var(--neon-blue);
  background:rgba(0,212,255,0.1);
  color:var(--neon-blue);
  transform:translateY(-3px);
  box-shadow:var(--glow-blue);
}
.contact-link svg{width:16px;height:16px;fill:currentColor}
.availability{
  display:inline-flex;align-items:center;gap:8px;
  font-family:'JetBrains Mono',monospace;font-size:11px;
  letter-spacing:2px;color:var(--neon-green);
  padding:10px 20px;
  border:1px solid rgba(0,255,157,0.2);
  background:rgba(0,255,157,0.05);
  margin-bottom:48px;
}
.availability::before{content:'';width:8px;height:8px;border-radius:50%;background:var(--neon-green);animation:pulse 2s infinite;box-shadow:0 0 12px var(--neon-green)}

/* FOOTER */
footer{
  position:relative;z-index:2;
  padding:40px 8%;
  border-top:1px solid var(--border);
  display:flex;align-items:center;justify-content:space-between;
  font-family:'JetBrains Mono',monospace;
  font-size:11px;color:var(--muted);
  flex-wrap:wrap;gap:20px;
}
.footer-logo{color:var(--neon-blue);letter-spacing:3px;font-family:'Orbitron',monospace;font-size:13px}
.footer-copy a{color:var(--neon-blue);text-decoration:none}

/* NOISE OVERLAY */
.noise{
  position:fixed;inset:0;z-index:3;pointer-events:none;
  opacity:0.025;
  background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='1'/%3E%3C/svg%3E");
  background-size:128px 128px;
}

/* SCANLINE */
.scanline{
  position:fixed;inset:0;z-index:4;pointer-events:none;
  background:repeating-linear-gradient(0deg,transparent,transparent 2px,rgba(0,0,0,0.02) 2px,rgba(0,0,0,0.02) 4px);
}

/* RESPONSIVE */
@media(max-width:768px){
  .about-grid{grid-template-columns:1fr}
  .hero-stats{gap:28px}
  nav .nav-links{display:none}
  .orbit-section{flex-direction:column}
  footer{flex-direction:column;text-align:center}
}

/* REVEAL */
.reveal{opacity:0;transform:translateY(40px);transition:opacity 0.8s,transform 0.8s}
.reveal.visible{opacity:1;transform:translateY(0)}

/* MAGNETIC */
.magnetic{display:inline-block;transition:transform 0.3s cubic-bezier(0.23,1,0.32,1)}
</style>
</head>
<body>

<div id="preloader">
  <div class="preload-text" id="preload-text">INITIALIZING SECURE CONNECTION</div>
  <div class="preload-bar-wrap"><div class="preload-bar" id="preload-bar"></div></div>
  <div class="preload-pct" id="preload-pct">0%</div>
</div>

<div id="cursor"></div>
<div id="cursor-ring"></div>
<canvas id="nebula"></canvas>
<div class="grid-overlay"></div>
<div class="noise"></div>
<div class="scanline"></div>

<nav id="navbar">
  <a href="#" class="nav-logo">VS</a>
  <ul class="nav-links">
    <li><a href="#about">ABOUT</a></li>
    <li><a href="#skills">ARSENAL</a></li>
    <li><a href="#projects">PROJECTS</a></li>
    <li><a href="#research">RESEARCH</a></li>
    <li><a href="#achievements">ACHIEVEMENTS</a></li>
    <li><a href="#contact">CONTACT</a></li>
  </ul>
</nav>

<!-- HERO -->
<section id="hero">
  <div class="hero-content">
    <div class="hero-badge">AVAILABLE FOR ENGAGEMENTS</div>
    <h1 class="hero-name">
      <span>VEDANT</span>
      <span>SAREEN</span>
    </h1>
    <div class="hero-role">
      <span id="typed-text"></span><span class="cursor-blink">_</span>
    </div>
    <p class="hero-desc">
      Cybersecurity undergraduate with hands-on experience in Web Application Penetration Testing, AI-driven security automation and technical customer support. Developed 20+ custom Python tools and reduced scan time by 30% through
multithreading optimization. Ranked in the Top 9% on TryHackMe and published contributor in AI-driven cybersecurity
research.
    </p>
    <div class="hero-cta">
      <a href="#contact" class="btn-primary magnetic">INITIATE CONTACT</a>
      <a href="#projects" class="btn-ghost magnetic">VIEW ARSENAL</a>
    </div>
    <div class="hero-stats">
      <div class="stat-item"><div class="stat-num" data-count="20">0</div><div class="stat-label">TOOLS BUILT</div></div>
      <div class="stat-item"><div class="stat-num" data-count="30">0</div><div class="stat-label">% SCAN SPEED ↑</div></div>
      <div class="stat-item"><div class="stat-num" data-count="8">0</div><div class="stat-label">TOP % TRYHACKME</div></div>
      
    </div>
  </div>
  <div class="hero-grid-num">
    <span>LAT: 30.9010</span>
    <span>LON: 75.8573</span>
    <span>LUDHIANA, IN</span>
  </div>
  <div class="scroll-hint" id="scroll-hint">
    <div class="scroll-line"></div>
    <span>SCROLL</span>
  </div>
</section>

<!-- ABOUT -->
<section id="about" class="reveal">
  <div class="section-header">
    <span class="section-tag">// 01 — IDENTITY</span>
    <h2 class="section-title">RED <em>TEAMER</em></h2>
    <div class="section-line"></div>
  </div>
  <div class="about-grid">
    <div class="about-visual">
      <div class="about-hex">
        <div class="about-hex-inner">
          [ VEDANT SAREEN ]<br/>
          CHITKARA UNIVERSITY<br/>
          BE CSE — CYBERSECURITY<br/>
          CGPA: 7.78<br/>
          STATUS: ACTIVE<br/>
          CLEARANCE: ETHICAL<br/>
          PATENT: FILED — PENDING
        </div>
      </div>
    </div>
    <div class="about-content">
      <p>Cybersecurity undergraduate engineering the intersection of offensive security and artificial intelligence. I build 20+ custom Python security utilities, all battle-tested on real environments.</p>
      <p>From my legacy of Cyber kill chain to Generative AI-powered vulnerability assessment pipelines, my work combines deep technical knowledge with automation-first thinking. Currently developing CyberXAgent — a patent-pending GenAI model for streamlined pentesting workflows.</p>
      <p>Published author for Cybersecurity in the
Age of AI and IoT. Ranked Top 8% globally on TryHackMe. CEH V13 AI in progress. The mission is simple: make security faster, smarter, and more precise.</p>
      <div class="about-tags">
        <span class="tag">OFFENSIVE SECURITY</span>
        <span class="tag">AI AUTOMATION</span>
        <span class="tag">PYTHON EXPERT</span>
        <span class="tag">VAPT</span>
        <span class="tag">MALWARE ANALYSIS</span>
        <span class="tag">DIGITAL FORENSICS</span>
        <span class="tag">RAG SYSTEMS</span>
        <span class="tag">OSINT</span>
      </div>
    </div>
  </div>
</section>

<!-- SKILLS -->
<section id="skills" class="reveal">
  <div class="section-header">
    <span class="section-tag">// 02 — TECHNICAL ARSENAL</span>
    <h2 class="section-title">COMBAT <em>STACK</em></h2>
    <div class="section-line"></div>
  </div>
  <div class="skills-container">
    <div class="skill-card">
      <div class="skill-card-icon">⚔️</div>
      <div class="skill-card-title">OFFENSIVE SECURITY</div>
      <div class="skill-list">
        <span class="skill-chip">Burp Suite</span>
        <span class="skill-chip">Metasploit</span>
        <span class="skill-chip">Nmap</span>
        <span class="skill-chip">SQLMap</span>
        <span class="skill-chip">Mimikatz</span>
        <span class="skill-chip">Bettercap</span>
        <span class="skill-chip">BeEF</span>
        <span class="skill-chip">Aircrack-ng</span>
        <span class="skill-chip">Hashcat</span>
        <span class="skill-chip">Hydra</span>
        <span class="skill-chip">OWASP ZAP</span>
        <span class="skill-chip">Wfuzz</span>
      </div>
    </div>
    <div class="skill-card">
      <div class="skill-card-icon">🧠</div>
      <div class="skill-card-title">AI & AUTOMATION</div>
      <div class="skill-list">
        <span class="skill-chip">Python (Advanced)</span>
        <span class="skill-chip">OpenAI API</span>
        <span class="skill-chip">RAG Systems</span>
        <span class="skill-chip">Multi-threading</span>
        <span class="skill-chip">MySQL</span>
        <span class="skill-chip">Bash Scripting</span>
        <span class="skill-chip">PSEXEC</span>
        <span class="skill-chip">Data Analysis</span>
      </div>
    </div>
    <div class="skill-card">
      <div class="skill-card-icon">🔍</div>
      <div class="skill-card-title">SIEM & ANALYSIS</div>
      <div class="skill-list">
        <span class="skill-chip">Wireshark</span>
        <span class="skill-chip">Snort</span>
        <span class="skill-chip">ELK Stack</span>
        <span class="skill-chip">IBM QRadar</span>
        <span class="skill-chip">Nessus</span>
        <span class="skill-chip">OSINT</span>
        <span class="skill-chip">CyberChef</span>
      </div>
    </div>
    <div class="skill-card">
      <div class="skill-card-icon">🏗️</div>
      <div class="skill-card-title">FRAMEWORKS</div>
      <div class="skill-list">
        <span class="skill-chip">OWASP Top 10</span>
        <span class="skill-chip">MITRE ATT&CK</span>
        <span class="skill-chip">CWE/CVE</span>
        <span class="skill-chip">CVSS</span>
        <span class="skill-chip">SHA/MD5</span>
        <span class="skill-chip">SSL/TLS</span>
        <span class="skill-chip">SAML</span>
        <span class="skill-chip">SSH</span>
      </div>
    </div>
    <div class="skill-card">
      <div class="skill-card-icon">💻</div>
      <div class="skill-card-title">OPERATING SYSTEMS</div>
      <div class="skill-list">
        <span class="skill-chip">Kali Linux</span>
        <span class="skill-chip">Ubuntu</span>
        <span class="skill-chip">Tails OS</span>
        <span class="skill-chip">Windows Internals</span>
        <span class="skill-chip">macOS</span>
        <span class="skill-chip">Privilege Escalation</span>
      </div>
    </div>
    <div class="skill-card">
      <div class="skill-card-icon">🌐</div>
      <div class="skill-card-title">NETWORKING</div>
      <div class="skill-list">
        <span class="skill-chip">TCP/IP</span>
        <span class="skill-chip">DNS Resolution</span>
        <span class="skill-chip">Packet Analysis</span>
        <span class="skill-chip">HTTP/HTTPS</span>
        <span class="skill-chip">Subnetting</span>
        <span class="skill-chip">ARP Spoofing</span>
      </div>
    </div>
  </div>
  <div class="orbit-section">
    <div class="orbit-container" id="orbit">
      <div class="orbit-ring" style="width:260px;height:260px"></div>
      <div class="orbit-ring" style="width:180px;height:180px;border-color:rgba(180,77,255,0.12)"></div>
      <div class="orbit-core">CYBER<br/>CORE</div>
    </div>
    <div class="orbit-label-list">
      <div class="orbit-label"><span class="orbit-dot"></span> Offensive Security Tools</div>
      <div class="orbit-label"><span class="orbit-dot purple"></span> AI / ML Security</div>
      <div class="orbit-label"><span class="orbit-dot green"></span> Network Security</div>
      <div class="orbit-label"><span class="orbit-dot"></span> SIEM & Forensics</div>
      <div class="orbit-label"><span class="orbit-dot purple"></span> Scripting & Automation</div>
      <div class="orbit-label"><span class="orbit-dot green"></span> End Point Security</div>
    </div>
  </div>
</section>

<!-- PROJECTS -->
<section id="projects" class="reveal">
  <div class="section-header">
    <span class="section-tag">// 03 — OPERATIONS</span>
    <h2 class="section-title">ACTIVE <em>DEPLOYMENTS</em></h2>
    <div class="section-line"></div>
  </div>
  <div class="projects-grid">
    <div class="project-card" id="pc1">
      <div class="project-num">01</div>
      <div class="project-category">AI SECURITY</div>
      <div class="project-title">CyberXAgent — GenAI Security Model</div>
      <div class="project-desc">Personalized GenAI model engineered to streamline vulnerability assessment. Leverages RAG to automate security report generation with recursive testing loops.</div>
      <div class="project-techs">
        <span class="tech-badge">Python</span>
        <span class="tech-badge">RAG</span>
        <span class="tech-badge">OpenAI API</span>
        <span class="tech-badge">OWASP LLM</span>
      </div>
      <ul class="project-highlights">
        <li>48% reduction in manual documentation time</li>
        <li>OWASP Top 10 + LLM-specific risk education platform</li>
        <li>Patent filed — Nov 2025 (Pending approval)</li>
      </ul>
    </div>
    <div class="project-card" id="pc2">
      <div class="project-num">02</div>
      <div class="project-category">NETWORK SECURITY</div>
      <div class="project-title">AI-Powered Security Automation Suite</div>
      <div class="project-desc">Portfolio of 20+ custom Python tools for network security testing — MAC Changer, Network Scanner, ARP Spoofer, DNS Spoofer and more. Multithreaded for maximum efficiency.</div>
      <div class="project-techs">
        <span class="tech-badge">Python</span>
        <span class="tech-badge">Scapy</span>
        <span class="tech-badge">Threading</span>
        <span class="tech-badge">Nmap</span>
      </div>
      <ul class="project-highlights">
        <li>30% reduction in network scan time via multithreading</li>
        <li>20+ production-ready offensive tools</li>
        <li>CyberSecGuru — Ludhiana (Mar 2023–Present)</li>
      </ul>
    </div>
    <div class="project-card" id="pc3">
      <div class="project-num">03</div>
      <div class="project-category">VAPT</div>
      <div class="project-title">BWAPP + DVWA Vulnerability Assessment</div>
      <div class="project-desc">Comprehensive vulnerability assessment across all difficulty modes in BWAPP and DVWA. PoC exploits, automated scanning and detailed remediation reports.</div>
      <div class="project-techs">
        <span class="tech-badge">DVWA</span>
        <span class="tech-badge">bWAPP</span>
        <span class="tech-badge">Burp Suite</span>
        <span class="tech-badge">SQLMap</span>
      </div>
      <ul class="project-highlights">
        <li>Full OWASP Top 10 vulnerability coverage</li>
        <li>CWE/CVE documented with CVSS scoring</li>
        <li>Validated PoC exploits + remediation roadmaps</li>
      </ul>
    </div>
  </div>
</section>

<!-- RESEARCH -->
<section id="research" class="reveal">
  <div class="section-header">
    <span class="section-tag">// 04 — INTELLIGENCE</span>
    <h2 class="section-title">RESEARCH & <em>PUBLICATIONS</em></h2>
    <div class="section-line"></div>
  </div>
  <div class="research-grid">
    <div class="research-card">
      <div class="research-type">📚 BOOK CHAPTER — PUBLISHED</div>
      <div class="research-title">"Future Research Directions and Open Challenges" — Chapter 14</div>
      <div class="research-meta">Cybersecurity in the Age of AI and IoT — June 2025</div>
    </div>
    <div class="research-card">
      <div class="research-type">📄 RESEARCH PAPER — IN PROGRESS</div>
      <div class="research-title">"Advances in Cyber Security – IoT and AI based solution deployed by cybersecurity"</div>
      <div class="research-meta">Jan 2026 — Pursuing Publication</div>
    </div>
    <div class="research-card">
      <div class="research-type">🔬 PATENT — FILED PENDING</div>
      <div class="research-title">CyberXAgent — AI-Driven Vulnerability Assessment Automation Model</div>
      <div class="research-meta">Filed November 2025 — Awaiting Approval</div>
    </div>
  </div>
</section>

<!-- ACHIEVEMENTS + EDUCATION -->
<section id="achievements" class="reveal">
  <div class="section-header">
    <span class="section-tag">// 05 — MISSION LOG</span>
    <h2 class="section-title">ACHIEVEMENTS &amp; <em>EDUCATION</em></h2>
    <div class="section-line"></div>
  </div>
  <div class="achievements-grid">
    <div class="achievement-card">
      <span class="achievement-icon">🏆</span>
      <div class="achievement-value">TOP 8%</div>
      <div class="achievement-label">TryHackMe Global Ranking<br/><small style="color:var(--muted);font-family:'JetBrains Mono',monospace;font-size:10px">Ranked July 2025</small></div>
    </div>
    <div class="achievement-card">
      <span class="achievement-icon">📖</span>
      <div class="achievement-value">PUBLISHED</div>
      <div class="achievement-label">Book Chapter Author<br/><small style="color:var(--muted);font-family:'JetBrains Mono',monospace;font-size:10px">Cybersecurity in the Age of AI & IoT</small></div>
    </div>
    <div class="achievement-card">
      <span class="achievement-icon">⚗️</span>
      <div class="achievement-value">PATENT</div>
      <div class="achievement-label">Filed & Pending<br/><small style="color:var(--muted);font-family:'JetBrains Mono',monospace;font-size:10px">CyberXAgent GenAI Model</small></div>
    </div>
    <div class="achievement-card">
      <span class="achievement-icon">🛠️</span>
      <div class="achievement-value">20+</div>
      <div class="achievement-label">Custom Security Tools<br/><small style="color:var(--muted);font-family:'JetBrains Mono',monospace;font-size:10px">Python — Production Grade</small></div>
    </div>
  </div>

  <div style="display:grid;grid-template-columns:1fr 1fr;gap:80px;margin-top:80px;align-items:start" class="edu-cert-grid">
    <div>
      <h3 style="font-family:'Orbitron',monospace;font-size:16px;letter-spacing:3px;color:var(--neon-blue);margin-bottom:36px">EDUCATION TIMELINE</h3>
      <div class="timeline">
        <div class="timeline-item">
          <div class="timeline-dot"></div>
          <div class="timeline-year">MAY 2023 — AUG 2027</div>
          <div class="timeline-title">BE Computer Science (Cybersecurity)</div>
          <div class="timeline-sub">Chitkara University, Rajpura</div>
          <div class="timeline-desc">CGPA: 7.78 | Specializing in offensive security, AI-driven security automation and network defense systems.</div>
        </div>
        <div class="timeline-item">
          <div class="timeline-dot"></div>
          <div class="timeline-year">MARCH 2022 — 2023</div>
          <div class="timeline-title">Class XII — Non-Medical</div>
          <div class="timeline-sub">BCM Arya Model School, Ludhiana</div>
          <div class="timeline-desc">Percentage: 76.5% | Foundation in mathematics and physics that underpins network and cryptographic understanding.</div>
        </div>
      </div>
    </div>
    <div>
      <h3 style="font-family:'Orbitron',monospace;font-size:16px;letter-spacing:3px;color:var(--neon-purple);margin-bottom:28px">CERTIFICATIONS</h3>
      <div class="cert-grid" style="grid-template-columns:1fr 1fr;margin-top:0">
        <div class="cert-card">
          <div class="cert-icon">🎓</div>
          <div class="cert-name">CEH V13 AI</div>
          <div class="cert-status">EC-Council — In Progress</div>
        </div>
        <div class="cert-card">
          <div class="cert-icon">✅</div>
          <div class="cert-name">ETHICAL HACKER</div>
          <div class="cert-status">CyberSecGuru — Certified</div>
        </div>
        <div class="cert-card">
          <div class="cert-icon">🔒</div>
          <div class="cert-name">OWASP TOP 10</div>
          <div class="cert-status">Practical — Validated</div>
        </div>
        <div class="cert-card">
          <div class="cert-icon">🎵</div>
          <div class="cert-name">MUSICIAN</div>
          <div class="cert-status">Mentor & Judge — 2019+</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CONTACT -->
<section id="contact" class="reveal">
  <div class="contact-wrapper">
    <div class="section-header" style="text-align:center">
      <span class="section-tag" style="display:block;text-align:center">// 06 — ESTABLISH LINK</span>
      <h2 class="section-title">OPEN <em>CHANNEL</em></h2>
      <div class="section-line" style="margin:16px auto 0"></div>
    </div>
    <div class="availability">AVAILABLE FOR PENTESTING ENGAGEMENTS & COLLABORATIONS</div>
    <p class="contact-tagline">
      Whether you need a skilled ethical hacker, a Python security tool builder, or a researcher pushing the boundaries of AI-driven security — let's connect and build something formidable.
    </p>
    <div class="contact-links">
      <a href="mailto:securecybernetics@gmail.com" class="contact-link magnetic">
        <svg viewBox="0 0 24 24"><path d="M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/></svg>
        securecybernetics@gmail.com
      </a>
      <a href="https://linkedin.com/in/sareen-cybersecurity-477180ved" target="_blank" class="contact-link magnetic">
        <svg viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
        LinkedIn Profile
      </a>
      <a href="https://github.com/CYBERSAREEN" target="_blank" class="contact-link magnetic">
        <svg viewBox="0 0 24 24"><path d="M12 .297c-6.63 0-12 5.373-12 12 0 5.303 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61C4.422 18.07 3.633 17.7 3.633 17.7c-1.087-.744.084-.729.084-.729 1.205.084 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 22.092 24 17.592 24 12.297c0-6.627-5.373-12-12-12"/></svg>
        CYBERSAREEN
      </a>
    </div>
    <a href="tel:+917087603933" class="contact-link magnetic" style="width:fit-content;margin:0 auto;display:flex">
      <svg viewBox="0 0 24 24" style="width:16px;height:16px;fill:currentColor"><path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/></svg>
      +91 70876 03933
    </a>
  </div>
</section>

<footer>
  <span class="footer-logo">VEDANT SAREEN</span>
  <span class="footer-copy">© 2026 — Engineered with precision in <a href="#">Ludhiana, Punjab</a></span>
  <span style="font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--muted)">ETHICAL HACKER · AI RESEARCHER · TOOL BUILDER</span>
</footer>

<script>
// === PRELOADER ===
(function(){
  const bar=document.getElementById('preload-bar');
  const pct=document.getElementById('preload-pct');
  const txt=document.getElementById('preload-text');
  const msgs=['INITIALIZING SECURE CONNECTION','LOADING THREAT MATRIX','ESTABLISHING CYBER VOID','DECRYPTING CREDENTIALS','ACCESS GRANTED'];
  let p=0,mi=0;
  gsap.to(txt,{opacity:1,duration:0.5});
  const iv=setInterval(()=>{
    p+=Math.random()*8+2;
    if(p>100)p=100;
    bar.style.width=p+'%';
    pct.textContent=Math.floor(p)+'%';
    if(p>25&&mi===0){mi=1;txt.textContent=msgs[1]}
    if(p>50&&mi===1){mi=2;txt.textContent=msgs[2]}
    if(p>75&&mi===2){mi=3;txt.textContent=msgs[3]}
    if(p>=100){
      clearInterval(iv);
      txt.textContent=msgs[4];
      setTimeout(()=>{
        gsap.to('#preloader',{opacity:0,duration:0.8,onComplete:()=>{
          document.getElementById('preloader').style.display='none';
          initHero();
        }});
      },500);
    }
  },60);
})();

// === NEBULA CANVAS ===
(function(){
  const c=document.getElementById('nebula');
  const ctx=c.getContext('2d');
  let W,H,particles=[];
  function resize(){W=c.width=window.innerWidth;H=c.height=window.innerHeight}
  resize();window.addEventListener('resize',resize);
  class Star{
    constructor(){this.reset()}
    reset(){
      this.x=Math.random()*W;this.y=Math.random()*H;
      this.r=Math.random()*1.5+0.2;
      this.vx=(Math.random()-0.5)*0.15;this.vy=(Math.random()-0.5)*0.15;
      this.a=Math.random();this.ta=Math.random();
      const colors=['rgba(0,212,255,','rgba(180,77,255,','rgba(0,255,157,','rgba(255,255,255,'];
      this.col=colors[Math.floor(Math.random()*colors.length)];
    }
    update(){
      this.x+=this.vx;this.y+=this.vy;
      this.a+=(this.ta-this.a)*0.02;
      if(Math.random()<0.001)this.ta=Math.random();
      if(this.x<0||this.x>W||this.y<0||this.y>H)this.reset();
    }
    draw(){
      ctx.beginPath();
      ctx.arc(this.x,this.y,this.r,0,Math.PI*2);
      ctx.fillStyle=this.col+this.a+')';
      ctx.fill();
    }
  }
  for(let i=0;i<220;i++)particles.push(new Star());
  let nebOff=0;
  function frame(){
    ctx.clearRect(0,0,W,H);
    nebOff+=0.001;
    const grd=ctx.createRadialGradient(W/2,H/2,0,W/2,H/2,W*0.7);
    grd.addColorStop(0,'rgba(0,20,50,0.4)');grd.addColorStop(0.4,'rgba(5,0,25,0.2)');grd.addColorStop(1,'rgba(0,0,0,0)');
    ctx.fillStyle=grd;ctx.fillRect(0,0,W,H);
    const g2=ctx.createRadialGradient(W*0.25+Math.sin(nebOff)*80,H*0.3+Math.cos(nebOff*1.3)*60,0,W*0.25,H*0.3,W*0.4);
    g2.addColorStop(0,'rgba(0,100,200,0.06)');g2.addColorStop(1,'transparent');
    ctx.fillStyle=g2;ctx.fillRect(0,0,W,H);
    const g3=ctx.createRadialGradient(W*0.75+Math.sin(nebOff*0.7)*60,H*0.65+Math.cos(nebOff)*40,0,W*0.75,H*0.65,W*0.35);
    g3.addColorStop(0,'rgba(100,0,180,0.05)');g3.addColorStop(1,'transparent');
    ctx.fillStyle=g3;ctx.fillRect(0,0,W,H);
    particles.forEach(p=>{p.update();p.draw()});
    for(let i=0;i<particles.length;i++){
      for(let j=i+1;j<particles.length;j++){
        const dx=particles[i].x-particles[j].x,dy=particles[i].y-particles[j].y;
        const d=Math.sqrt(dx*dx+dy*dy);
        if(d<80){
          ctx.beginPath();
          ctx.moveTo(particles[i].x,particles[i].y);
          ctx.lineTo(particles[j].x,particles[j].y);
          ctx.strokeStyle='rgba(0,212,255,'+(0.03*(1-d/80))+')';
          ctx.lineWidth=0.5;ctx.stroke();
        }
      }
    }
    requestAnimationFrame(frame);
  }
  frame();
})();

// === CURSOR ===
const cur=document.getElementById('cursor'),ring=document.getElementById('cursor-ring');
let mx=0,my=0,rx=0,ry=0;
document.addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY;cur.style.left=mx+'px';cur.style.top=my+'px'});
(function lerp(){rx+=(mx-rx)*0.12;ry+=(my-ry)*0.12;ring.style.left=rx+'px';ring.style.top=ry+'px';requestAnimationFrame(lerp)})();
document.querySelectorAll('a,button,.magnetic,.project-card,.skill-card').forEach(el=>{
  el.addEventListener('mouseenter',()=>{cur.style.transform='translate(-50%,-50%) scale(2.5)';ring.style.width='60px';ring.style.height='60px'});
  el.addEventListener('mouseleave',()=>{cur.style.transform='translate(-50%,-50%) scale(1)';ring.style.width='40px';ring.style.height='40px'});
});

// === SCROLL NAV ===
window.addEventListener('scroll',()=>{
  document.getElementById('navbar').classList.toggle('scrolled',window.scrollY>50);
});

// === HERO INIT ===
function initHero(){
  gsap.to('.hero-badge',{opacity:1,y:0,duration:0.6,delay:0.2});
  gsap.to('.hero-name',{opacity:1,y:0,duration:1,delay:0.4,ease:'power4.out'});
  gsap.to('.hero-role',{opacity:1,duration:0.8,delay:0.8});
  gsap.to('.hero-desc',{opacity:1,duration:0.8,delay:1});
  gsap.to('.hero-cta',{opacity:1,y:0,duration:0.6,delay:1.2});
  gsap.to('.hero-stats',{opacity:1,duration:0.6,delay:1.4});
  gsap.to('#scroll-hint',{opacity:1,duration:0.6,delay:2});
  setTimeout(typeWriter,1400);
  setTimeout(animateCounters,1600);
}

// === TYPED TEXT ===
const roles=['PENETRATION TESTER','SECURITY RESEARCHER','PYTHON TOOL BUILDER','AI SECURITY ENGINEER','ETHICAL HACKER'];
let ri=0,ci=0,del=false;
function typeWriter(){
  const el=document.getElementById('typed-text');
  const cur=roles[ri];
  if(!del){
    el.textContent=cur.substring(0,ci+1);ci++;
    if(ci===cur.length){del=true;setTimeout(typeWriter,1800);return}
  }else{
    el.textContent=cur.substring(0,ci-1);ci--;
    if(ci===0){del=false;ri=(ri+1)%roles.length}
  }
  setTimeout(typeWriter,del?50:90);
}

// === COUNTERS ===
function animateCounters(){
  document.querySelectorAll('[data-count]').forEach(el=>{
    const target=+el.dataset.count;
    let current=0;
    const inc=target/40;
    const timer=setInterval(()=>{
      current+=inc;
      if(current>=target){current=target;clearInterval(timer)}
      el.textContent=Math.floor(current)+(el.parentElement.querySelector('.stat-label').textContent.includes('%')?'%':'');
    },35);
  });
}

// === SCROLL REVEALS ===
const reveals=document.querySelectorAll('.reveal');
const observer=new IntersectionObserver(entries=>{
  entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible')}});
},{threshold:0.1});
reveals.forEach(r=>observer.observe(r));

// === GSAP SCROLL TRIGGERS ===
gsap.registerPlugin(ScrollTrigger);
document.querySelectorAll('.skill-card,.project-card,.research-card,.achievement-card,.timeline-item').forEach((el,i)=>{
  gsap.from(el,{opacity:0,y:50,duration:0.7,delay:i*0.1,ease:'power2.out',
    scrollTrigger:{trigger:el,start:'top 85%',once:true}
  });
});

// === 3D TILT ===
document.querySelectorAll('.project-card').forEach(card=>{
  card.addEventListener('mousemove',e=>{
    const r=card.getBoundingClientRect();
    const x=e.clientX-r.left,y=e.clientY-r.top;
    const cx=r.width/2,cy=r.height/2;
    const dx=(x-cx)/cx,dy=(y-cy)/cy;
    card.style.transform=`perspective(800px) rotateY(${dx*8}deg) rotateX(${-dy*8}deg) translateY(-8px)`;
    card.style.setProperty('--mx',(x/r.width*100)+'%');
    card.style.setProperty('--my',(y/r.height*100)+'%');
  });
  card.addEventListener('mouseleave',()=>{card.style.transform='perspective(800px) rotateY(0) rotateX(0)'});
});

// === MAGNETIC BUTTONS ===
document.querySelectorAll('.magnetic').forEach(el=>{
  el.addEventListener('mousemove',e=>{
    const r=el.getBoundingClientRect();
    const x=(e.clientX-r.left-r.width/2)*0.35;
    const y=(e.clientY-r.top-r.height/2)*0.35;
    el.style.transform=`translate(${x}px,${y}px)`;
  });
  el.addEventListener('mouseleave',()=>{el.style.transform='translate(0,0)'});
});

// === ORBIT ANIMATION ===
(function(){
  const c=document.getElementById('orbit');
  if(!c)return;
  const icons=['🔓','🐍','🌐','🔍','⚡','🧠'];
  const r1=130,r2=90;
  const planets=icons.map((icon,i)=>{
    const div=document.createElement('div');
    div.className='orbit-planet';
    div.innerHTML=icon;
    c.appendChild(div);
    return{el:div,r:i<3?r1:r2,offset:(i/(i<3?3:3))*Math.PI*2+(i<3?0:Math.PI/6),speed:i<3?0.4:0.7};
  });
  let t=0;
  function animate(){
    t+=0.008;
    planets.forEach(p=>{
      const angle=p.offset+t*p.speed;
      const x=160+p.r*Math.cos(angle)-22;
      const y=160+p.r*Math.sin(angle)-22;
      p.el.style.left=x+'px';p.el.style.top=y+'px';
    });
    requestAnimationFrame(animate);
  }
  animate();
})();

// === EDU GRID RESPONSIVE ===
function checkEduGrid(){
  const g=document.querySelector('.edu-cert-grid');
  if(g&&window.innerWidth<768)g.style.gridTemplateColumns='1fr';
  else if(g)g.style.gridTemplateColumns='1fr 1fr';
}
checkEduGrid();window.addEventListener('resize',checkEduGrid);
</script>
</body>
</html>
